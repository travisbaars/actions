name: Integration
on:
  workflow_call:

env:
  CARGO_TERM_COLOR: always

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Test (standard)
      - uses: travisbaars/actions/rust/std-test.yml@main
